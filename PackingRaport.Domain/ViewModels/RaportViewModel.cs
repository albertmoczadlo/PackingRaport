using AutoMapper;
using PackingRaport.Domain.Models;
using System;
using System.Collections;
using System.Collections.Generic;
using System.Linq;
using System.Runtime;
using System.Text;
using System.Threading.Tasks;

namespace PackingRaport.Domain.ViewModels
{
    public class RaportViewModel : Raport
    {
        //    public DateTime StartProductionTime { get; set; }
        //    public DateTime? EndProductionTime { get; set; }

        //    public User User { get; set; }
        //    public int Product { get; set; }
        //    public ICollection<Container> Containers { get; set; }

        //    public void Mapping(Profile profile)
        //    {
        //        profile.CreateMap<RaportViewModel, Raport>().ForMember(x => x.User, d => d.MapFrom(r => r.User))
        //            .ForMember(x => x.Product, d => d.MapFrom(r => ))
        //            .ForMember(x => x.StartProductionTime, d => d.MapFrom(x => x.StartProductionTime))
        //            .ForMember(x => x.EndProductionTime, d => d.MapFrom(r => r.EndProductionTime));

        //    }
        //}
        //public IEnumerator GetEnumerator()
        //{
        //    throw new NotImplementedException();
        //}
    }
}
